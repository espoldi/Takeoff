import React from 'react';
// Material UI
import { Container } from '@material-ui/core';

export default function Form() {
    return (
        <Container fixed>
            <h1>FORM</h1>

        </Container>
    );
}